

export const eventReg = /^\$(on|catch)([A-Za-z_]+)(capture)?$/;

export const commonEventReg = /^\$(on|catch)(tap|touchstart|touchmove|touchend|touchcancel|transitionend|animationstart|animationiteration|animationend)(capture)?$/;